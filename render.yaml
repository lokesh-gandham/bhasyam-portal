services:
  - type: web
    name: bhasyam-portal
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/server/server.js
    envVars:
      - key: NITRO_PORT
        value: 10000
      - key: NITRO_PRESET
        value: node-server
